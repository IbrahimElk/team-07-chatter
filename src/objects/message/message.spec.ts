// import { Message } from './message.js';
// import { User } from '../user/user.js';
import { describe, it, expect } from 'vitest';
// import { MockWebSocket } from '../../protocol/__mock__/ws-mock.js';

describe('Message', () => {
  //   const user = new User('Jan', 'mijnAndereWachtwoord', '@Jan');
  //   user.setWebsocket(new MockWebSocket('FAKE_URL_2'));
  //   const date = new Date().getTime().toString();
  //   const text = 'Hello, world!';
  //   const MUID = '12345';

  //   const message = new Message(user.getName(), date, text, MUID);

  it('should retrieve the correct MUID', () => {
    //     expect(message.getMUID()).toBe(MUID);
  });

  //   it('should retrieve the correct user name', () => {
  //     expect(message.getUserName()).toBe(user.getName());
  //   });

  //   it('should retrieve the correct date', () => {
  //     expect(message.getDate()).toBe(date);
  //   });

  //   it('should retrieve the correct text', () => {
  //     expect(message.getText()).toBe(text);
  //   });
});
